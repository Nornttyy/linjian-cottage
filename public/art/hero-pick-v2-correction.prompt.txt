Targeted correction of this pickaxe animation sheet, keep all 24 sprites and the existing character identity, pixel style, costume, proportions, grid and placement. Exactly 8 columns x3 rows. Change ONLY pose column FIVE (zero-based frame4) in EACH of the three rows: currently this column is already at ground impact, so there is no intermediate descending pose. Instead show the tool about halfway down its arc: both hands about forehead to upper chest height, the metal pick head moving from overhead towards the facing target, with its handle approximately diagonal 35 degrees above horizontal. DOWN row head/torso still upright with only a slight forward lean and boots planted; UP row same rear orientation, visible descending tool head just outside the top-right shoulder, never a face; RIGHT row strict right profile, hands at upper chest, pick head to the right around shoulder height, diagonal forward handle. This column must visually interpolate between column4 overhead and column6 low ground impact. The low contact pose belongs only to column6. Keep columns1-4 and6-8 unchanged. Do not change character size or foot baseline. Additionally replace all checkerboard background with PERFECTLY SOLID FLAT PURE MAGENTA #ff00ff. No checkerboard or textured backdrop anywhere, no shadows, no text/grid. Crisp pixel-art edges.
