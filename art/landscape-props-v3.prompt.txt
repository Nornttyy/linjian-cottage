Built-in image_gen; reference-preserving material simplification. Original generated PNG bytes retained.

Use case: precise-object-edit
Repaint THIS SAME existing sprite atlas with visibly coarser pixels and much less surface detail, keeping all recognizable designs. Do not replace the objects. Keep exactly the current 8 sprites, their positions, relative sizes, silhouette outlines, trunks and branch shapes, canopy lobes and color palettes. Four trees oak/birch/golden-maple/snow-pine in the tall upper band, berry bush/mossy rocks/white-yellow flowers/pink-purple flowers in the shorter lower band. Preserve four columns, full original canvas composition and transparent gaps between sprites.
The prior 40-percent simplification was still too fine. This time eliminate about 75 percent of the tiny leaf/bark/stone/snow flecks. Redraw each tree as if built on a genuinely coarse 48-pixel-wide by 64-pixel-tall logical grid, enlarged crisply in this atlas. Main original canopy lobes remain in place but contain only a few large solid shaded pixel clusters, not hundreds of individual leaves. Keep about 5-7 original shades per tree including its existing highlights and shadow hue. Bark: broad 2-3-color planes on the same trunk and branches; no grain. Snow: same shelves and silhouette with 3 plain stepped shades. Rocks: same masses with simple planes and moss shapes. Flowers and berries still distinct, each petal/fruit represented with a few large square pixel clusters. Preserve the current calm classic farming-RPG mood and bright original coloring, not geometric icon blobs, not a new cartoon design. No blur or smooth gradients, no added dark outlines.
Output complete edited sprite atlas, real transparent alpha background, exact original framing, no black/white/checkerboard backdrop, no labels.

Background-only repair:
Edit ONLY the background of this sprite atlas. The grey-and-white checkerboard is accidentally baked into the RGB image. Remove that entire checkerboard and output a PNG with a REAL TRANSPARENT ALPHA CHANNEL, with alpha=0 outside the sprites and in all gaps. This must be genuine transparency, not a drawn transparency pattern and not a white or black background. Keep ALL eight coarse pixel sprites exactly as they are: unchanged pixel blocks, colors, outlines, geometry, positions, canvas dimensions and framing. Preserve every white birch bark pixel, snow pixel, flower and gray stone inside the sprites. Only remove the backdrop. The scene must be compositable onto an arbitrary game background without any square backdrop or fringe. No other edits.

Final background-only chroma edit:
Use case: precise-object-edit
Change ONLY the background of this pixel sprite atlas. Replace ALL of the gray-white checkerboard with one perfectly solid flat pure magenta color #FF00FF (RGB 255,0,255). This magenta is a technical color-key backdrop, not a lighting color: no gradients, no pattern, no shadows, no magenta on the sprites. Preserve all eight objects EXACTLY as shown, same coarse pixel clusters, colors, white birch bark and snow, flowers, leaves, positions, sizes, 4 columns and uneven tall upper/short lower bands. Keep existing canvas dimensions and framing. Every gap between branches and between objects should show the exact same pure magenta #FF00FF. Sharp pixel edges, no semi-transparent fringe, no checkerboard, no text. Do not refine or add any detail to the objects.
